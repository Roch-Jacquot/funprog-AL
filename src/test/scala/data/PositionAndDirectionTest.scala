package data

import org.scalatest.funsuite.AnyFunSuite

class PositionAndDirectionTest extends AnyFunSuite {

  val positionAndDirection = PositionAndDirection(Point(1, 1), "N")

  test(
    "updatePosition Something something dark"
  ) {
    val result =
  }

  test(
    "updateDirection Something something dark"
  ) {

  }
}
